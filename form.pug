doctype html
html(lang="ja")
  head
    meta(charset="UTF-8")
    title アンケート

  body
    h1 どちらが食べたいですか？
    form(method="post" action=path)

      input(type="radio" name="favorite" value=firstItem)
      span #{firstItem}
    
      input(type="radio" name="fovorite" value=secondItem)
      span #{secondItem}
      br
      hr

      span 名前:
      input(type="text" name="name")

      button(type="submit") 投稿